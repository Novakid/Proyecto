# Frontend

Aplicación Vue/Vite del proyecto Aparicio Gestión. Consulte la [documentación principal](../README.md) para configuración de `VITE_API_URL`, desarrollo, build y empaquetado con Electron.

En navegador usa history normal; cuando Electron carga el build mediante `file://`, cambia automáticamente a hash history.
